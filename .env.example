# Database Configuration
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://postgres:password@localhost:5432/retain_dev

# Individual connection parameters (alternative to DATABASE_URL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=retain_dev
DB_USER=postgres
DB_PASSWORD=password

# Environment
ENVIRONMENT=development

# Logging
LOG_LEVEL=INFO

# Data paths
DATA_RAW_PATH=data/raw
DATA_PROCESSED_PATH=data/processed

# Model artifacts
MODEL_PATH=models/

# Optional: MLflow tracking (for later)
# MLFLOW_TRACKING_URI=http://localhost:5000

# Optional: Email service for interventions (for later)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASSWORD=
